# AILearn Hub 后端环境变量配置示例
# 复制此文件为 .env 并填写实际值

# ==================== LLM 通用配置 ====================
# 用于 AI 助教、质量评估等场景
LLM_API_KEY=your-api-key-here
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-3.5-turbo

# ==================== Langfuse 监控（可选） ====================
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=https://cloud.langfuse.com

# ==================== RAG Embedding 配置 ====================
# 提供商选择: openai | local | custom
RAG_EMBEDDING_PROVIDER=openai

# OpenAI 模式
RAG_OPENAI_API_KEY=your-openai-api-key
RAG_OPENAI_BASE_URL=https://api.openai.com/v1
RAG_EMBEDDING_MODEL=text-embedding-3-small

# 本地模式（Ollama）
# RAG_EMBEDDING_PROVIDER=local
# RAG_EMBEDDING_SERVICE_URL=http://localhost:11434/api/embeddings
# RAG_EMBEDDING_LOCAL_MODEL=nomic-embed-text

# ==================== RAG 检索策略配置 ====================
# 检索模式: vector | vector_rerank | hybrid
RAG_RETRIEVAL_MODE=vector

# ==================== RAG Rerank 配置（可选） ====================
# 注意：Rerank与Embedding独立，因为Ollama对Rerank支持有限
RAG_RERANK_ENABLED=false
RAG_RERANK_PROVIDER=local

# 本地 Rerank 服务
# RAG_RERANK_SERVICE_URL=http://localhost:8002/rerank

# Cohere Rerank API
# RAG_RERANK_PROVIDER=cohere
# RAG_RERANK_COHERE_API_KEY=your-cohere-key

# ==================== 数据库配置 ====================
DATABASE_URL=postgresql://user:password@localhost:5432/ailearn

# ==================== Redis 配置 ====================
REDIS_URL=redis://localhost:6379/0
