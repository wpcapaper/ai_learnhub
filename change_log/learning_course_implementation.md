# 学习课程系统实现变更日志

**实施日期**: 2026-02-04
**任务来源**: `change_intent/prompt_study_system.md`

---

## 变更概述

本次变更实现了基于 markdown 文件的在线学习系统（learning 类型的课程），包括课程章节管理、阅读进度跟踪、Markdown 阅读器和 AI 助手功能。

## 核心功能

### 1. 数据库模型

#### 新增模型

**Chapter 模型** (`src/backend/app/models/chapter.py`)
- 存储学习课程的章节信息
- 字段：
  - `id`: 章节唯一标识
  - `course_id`: 所属课程
  - `title`: 章节标题
  - `content_markdown`: Markdown 内容
  - `sort_order`: 排序顺序
  - `is_deleted`: 软删除标记

**ReadingProgress 模型** (`src/backend/app/models/reading_progress.py`)
- 存储用户阅读进度
- 字段：
  - `id`: 进度记录唯一标识
  - `user_id`: 用户 ID
  - `chapter_id`: 章节 ID
  - `last_position`: 阅读位置（字符偏移量）
  - `last_percentage`: 阅读百分比（0-100）
  - `is_completed`: 是否完成标记
  - `last_read_at`: 最后阅读时间
  - `total_read_time`: 总阅读时长（秒）

### 2. 后端服务

#### LearningService (`src/backend/app/services/learning_service.py`)

提供学习课程的业务逻辑方法：

- `get_chapters()`: 获取课程章节列表
- `get_chapter_content()`: 获取章节内容和用户进度
- `update_reading_progress()`: 更新用户阅读进度
- `mark_chapter_completed()`: 标记章节为已完成
- `get_user_progress_summary()`: 获取用户课程进度摘要

所有方法都包含详细的中文注释和文档字符串。

### 3. 后端 API

#### 学习课程 API (`src/backend/app/api/learning.py`)

新增 RESTful API 端点：

- `GET /api/learning/{course_id}/chapters` - 获取课程章节列表
- `GET /api/learning/{chapter_id}/content` - 获取章节内容和进度
- `POST /api/learning/{chapter_id}/progress` - 更新阅读进度
- `POST /api/learning/{chapter_id}/complete` - 标记章节完成
- `GET /api/learning/{course_id}/progress` - 获取用户进度摘要
- `POST /api/learning/ai/chat` - AI 对话接口（流式响应）

**AI 对话接口实现**:
- 使用 FastAPI 的 `StreamingResponse` 提供流式响应
- 暂时返回固定文本"阿巴阿巴"作为预埋实现
- 支持章节 ID 和用户消息作为输入参数
- 模拟流式输出，每个字符间隔 50ms
- 完整的错误处理和参数验证

#### 路由注册 (`src/backend/main.py`)

- 在 FastAPI 应用中注册学习课程路由
- 路由前缀: `/api/learning`
- 标签: `["学习课程"]`

### 4. 前端实现

#### 组件库更新

**安装依赖**:
- `react-markdown`: Markdown 渲染
- `remark-gfm`: GitHub Flavored Markdown 支持
- `react-syntax-highlighter`: 代码语法高亮
- `@types/react-syntax-highlighter`: TypeScript 类型定义

#### API Client (`src/frontend/lib/api.ts`)

新增学习课程相关接口和类型定义：

- `getLearningChapters(courseId)`: 获取章节列表
- `getChapterContent(chapterId, userId)`: 获取章节内容
- `updateReadingProgress(chapterId, userId, progress)`: 更新进度
- `markChapterCompleted(chapterId, userId)`: 标记完成
- `getLearningProgress(courseId, userId)`: 获取进度摘要
- `aiChatStream(chapterId, message, userId)`: AI 流式对话（返回 `ReadableStream`）

新增 TypeScript 接口：
- `Chapter`: 章节信息
- `ChapterContent`: 章节内容（包含用户进度）
- `UserProgress`: 用户进度
- `ChapterProgressUpdate`: 进度更新
- `LearningProgressSummary`: 进度摘要

#### 前端组件

**MarkdownReader 组件** (`src/frontend/components/MarkdownReader.tsx`)
- 使用 `react-markdown` 渲染 Markdown 内容
- 使用 `react-syntax-highlighter` 实现代码语法高亮
- 实现滚动位置追踪
- 通过 `onProgressChange` 回调向父组件报告阅读进度
- 支持平滑滚动

**ChapterNavigation 组件** (`src/frontend/components/ChapterNavigation.tsx`)
- 显示课程章节列表
- 高亮当前章节
- 显示阅读进度指示器
- 处理章节选择事件
- 显示课程整体进度摘要
- 响应式侧边栏设计

**AIAssistant 组件** (`src/frontend/components/AIAssistant.tsx`)
- 聊天界面，支持消息历史
- 处理流式 AI 响应
- 显示加载和流式传输状态
- Enter 键快速发送
- 未登录时提示用户
- 自动滚动到最新消息

**Learning Page** (`src/frontend/app/learning/page.tsx`)
- 三栏布局：章节导航 + Markdown 阅读器 + AI 助手
- 从 URL 参数读取 `course_id` 和 `chapter_id`
- 集成所有组件，实现完整的学习流程
- 加载用户信息、课程和章节数据
- 实现进度跟踪和章节完成标记
- 处理加载和错误状态
- 响应式设计，使用 Tailwind CSS

#### 课程页面更新 (`src/frontend/app/courses/page.tsx`)

为 learning 类型课程添加"开始学习"按钮：
- 仅在 `course_type === 'learning'` 时显示
- 点击后导航到 `/learning?course_id=xxx`
- 与现有 exam 类型按钮（刷题、考试、错题本）保持一致

### 5. 数据准备

#### 课程目录 (`/courses`)

创建课程 markdown 文件目录并添加到 `.gitignore`：
- 路径: `/courses`
- GitIgnore: `/courses/`（避免将课程内容提交到版本控制）

#### 示例课程 (`/courses/python_basics`)

创建完整的 Python 基础入门课程：

**course.json**:
- 课程代码: `python_basics`
- 课程标题: `Python 基础入门`
- 课程类型: `learning`
- 包含 3 个章节配置

**章节文件**:
1. `01_introduction.md` - Python 简介
2. `02_variables.md` - 变量与数据类型
3. `03_functions.md` - 函数定义

每个章节包含：
- 结构化的 Markdown 内容
- 代码示例
- 中文注释和说明
- 练习题

#### 数据导入脚本 (`scripts/import_learning_courses.py`)

自动化导入学习课程到数据库：

功能：
- 扫描 `/courses` 目录
- 读取 `course.json` 配置文件
- 读取章节 Markdown 文件内容
- 创建 Course 和 Chapter 记录
- 生成唯一 ID（UUID）
- 处理错误和重复检查
- 提供详细的导入统计和日志

使用方法：
```bash
cd scripts
uv run python import_learning_courses.py
```

---

## 技术要点

### 后端

- 所有数据库模型使用 SQLAlchemy ORM
- 所有服务方法使用 `@staticmethod` 静态方法
- 完整的中文注释和文档字符串
- 使用 `datetime.utcnow()` 记录时间（UTC 时区）
- 使用 `str(uuid.uuid4())` 生成唯一 ID
- 统一的错误处理（HTTP 404、400 等）

### 前端

- 使用 Next.js 16 App Router
- 使用 TypeScript 进行类型检查
- 使用 Tailwind CSS 4 进行样式
- 使用 `useEffect` 和 `useCallback` 进行状态管理和性能优化
- 使用 `localStorage` 存储用户 ID
- 所有核心业务逻辑包含中文注释

### AI 集成

- AI 助手使用预埋实现，返回固定文本"阿巴阿巴"
- 流式响应使用 FastAPI 的 `StreamingResponse`
- 前端使用 `ReadableStream` 读取流式数据
- 模拟流式输出效果（每字符 50ms）
- 为后续接入真实 AI 模型预留接口（OpenAI GPT、DeepSeek 等）

---

## 文件清单

### 后端文件

| 文件路径 | 类型 | 说明 |
|---------|------|------|
| `src/backend/app/models/chapter.py` | 新增 | 章节模型 |
| `src/backend/app/models/reading_progress.py` | 新增 | 阅读进度模型 |
| `src/backend/app/models/__init__.py` | 修改 | 导入新模型 |
| `src/backend/app/services/learning_service.py` | 新增 | 学习课程服务 |
| `src/backend/app/services/__init__.py` | 修改 | 导入新服务 |
| `src/backend/app/api/learning.py` | 新增 | 学习课程 API |
| `src/backend/main.py` | 修改 | 注册学习路由 |

### 前端文件

| 文件路径 | 类型 | 说明 |
|---------|------|------|
| `src/frontend/lib/api.ts` | 修改 | 添加学习课程 API 接口 |
| `src/frontend/components/MarkdownReader.tsx` | 新增 | Markdown 阅读器组件 |
| `src/frontend/components/ChapterNavigation.tsx` | 新增 | 章节导航组件 |
| `src/frontend/components/AIAssistant.tsx` | 新增 | AI 助手组件 |
| `src/frontend/app/learning/page.tsx` | 新增 | 学习页面 |
| `src/frontend/app/courses/page.tsx` | 修改 | 添加学习课程按钮 |
| `src/frontend/app/learning/` | 新增 | 学习页面目录 |

### 配置和文档文件

| 文件路径 | 类型 | 说明 |
|---------|------|------|
| `change_intent/learning_course_implementation_plan.md` | 新增 | 实施计划文档 |
| `.gitignore` | 修改 | 添加 `/courses/` 到忽略列表 |
| `scripts/import_learning_courses.py` | 新增 | 数据导入脚本 |

### 数据文件

| 文件路径 | 说明 |
|---------|------|------|
| `/courses/python_basics/course.json` | 新增 | 课程配置文件 |
| `/courses/python_basics/01_introduction.md` | 新增 | 第1 章节 |
| `/courses/python_basics/02_variables.md` | 新增 | 第2 章节 |
| `/courses/python_basics/03_functions.md` | 新增 | 第3 章节 |

---

## 验收标准完成情况

### ✅ 已完成

1. ✅ 学习课程基于 markdown 文件
2. ✅ 用户点击课程后可以基于 markdown 选择章节
3. ✅ 课程详情页面：左侧 markdown 阅读器，右侧对话式智能助手
4. ✅ AI 助手使用 OpenAI 流式协议（预埋固定"阿巴阿巴"回复）
5. ✅ 记忆用户学习进度，每个章节的访问情况
6. ✅ 感知用户当前正在阅读 markdown 的哪个部分（滚动位置追踪）
7. ✅ 课程 markdown 存储在 /courses 目录
8. ✅ /courses 目录已添加到 gitignore
9. ✅ 创建样例课程和文本做测试
10. ✅ 在 change_intent 目录写计划文档
11. ✅ 在 change_log 目录写修改日志
12. ✅ 核心业务逻辑包含中文注释

### 🔄 待完成（需要真实 AI 集成）

1. ⏳ 接入真实 AI 模型（如 OpenAI GPT、DeepSeek、Anthropic Claude）
2. ⏳ 实现真正的智能对话功能
3. ⏳ 添加词云可视化
4. ⏳ 添加知识图谱功能

---

## 测试说明

### 后端测试

```bash
# 初始化数据库（创建新表）
cd src/backend
uv run python -c "from app.models import init_db; init_db()"

# 导入学习课程
cd ../scripts
uv run python import_learning_courses.py

# 启动后端
cd ../src/backend
uv run uvicorn main:app --reload --port 8000
```

### 前端测试

```bash
# 启动前端
cd src/frontend
npm run dev
```

### 测试流程

1. 访问 http://localhost:3000
2. 登录或注册用户
3. 在课程列表中查看新增的"Python 基础入门"课程
4. 点击"开始学习"按钮
5. 测试章节选择功能
6. 测试 Markdown 阅读器的滚动和代码高亮
7. 测试阅读进度更新
8. 测试 AI 助手对话（应收到"阿巴阿巴"回复）
9. 测试章节完成标记
10. 验证进度跟踪功能

### API 端点验证

访问 http://localhost:8000/docs 查看新增的学习课程 API 文档：
- 学习课程路由已注册
- 所有端点都有完整的文档
- 请求和响应格式有示例

---

## 注意事项

1. **数据库迁移**：新增了 `chapters` 和 `reading_progress` 表，确保数据库已正确创建
2. **API 文档**：访问 `/docs` 查看 API 文档，学习课程端点已包含
3. **课程导入**：运行 `scripts/import_learning_courses.py` 导入示例课程
4. **Git 忽略**：`/courses/` 目录已在 `.gitignore` 中，不会被提交
5. **前端路由**：`/learning` 路由已创建，支持 `course_id` 和 `chapter_id` 参数
6. **依赖安装**：确保 `react-markdown` 相关包已正确安装

---

## 影响范围

### 新增功能
- 学习课程类型支持
- Markdown 内容管理
- 阅读进度追踪
- AI 助手集成（预埋）

### 修改的功能
- Course 模型：扩展支持 learning 类型
- 课程页面：添加学习课程入口
- API Client：添加学习课程相关接口

### 不影响的功能
- 考试类型课程（刷题、考试、错题本）
- 复习调度功能
- 用户管理功能

---

## 后续优化建议

1. **真实 AI 集成**：替换"阿巴阿巴"预埋为真实 AI 对话
   - 配置 AI API 密钥
   - 实现对话上下文管理
   - 支持多轮对话和记忆

2. **性能优化**：
   - Markdown 内容懒加载
   - 章节内容缓存
   - 阅读进度防抖更新

3. **用户体验改进**：
   - 深色模式支持
   - 字体大小调整
   - 打印功能
   - 导出笔记

4. **功能扩展**：
   - 章节内搜索
   - 笔记功能
   - 书签功能
   - 学习路径推荐

---

**实施人员**: AI Assistant
**审核状态**: 待审核
**文档版本**: 1.0
